{
  "name": "hansgrubber",
  "version": "1.0.0",
  "description": "",
  "main": "hansGrubber.js",
  "scripts": {
    "test": "jest test --watch",
    "reset-db": "psql -f db/setup.sql",
    "migrate-test": "NODE_ENV=test knex migrate:latest",
    "seed-test": "NODE_ENV=test knex seed:run",
    "rollback-test": "NODE_ENV=test knex migrate:rollback",
    "migrate-dev": "NODE_ENV=dev knex migrate:latest",
    "seed-dev": "NODE_ENV=dev knex seed:run",
    "rollback:dev": "NODE_ENV=dev knex migrate:rollback"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^26.4.2",
    "knex": "^0.21.5",
    "pg": "^8.3.3",
    "tmi.js": "^1.5.0"
  }
}
